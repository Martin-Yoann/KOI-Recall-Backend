# Consumer disposal — acceptance matrix

Spec §7.1. One row per acceptance criterion, with the evidence that exists **today**.
A criterion is only marked proven when there is a concrete artefact to point at: a
named test, or a browser action whose write was confirmed in the database. Everything
else says so, because an acceptance record that overstates is worse than none.

Legend

- **proven** — a named test or a browser action with a database-confirmed write.
- **implemented, unproven** — the code path exists and is described, but nothing
  executes it in a way that would fail if it broke.
- **not built** — no code path.

## Proven

| #   | Criterion                                                                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D01 | A potential match produces no actionable task and no permission                                     | `tests/disposal-service.integration.test.ts` — seeds every product unconfirmed and asserts the authorization is refused with `ELIGIBILITY_NOT_CONFIRMED`; `tests/disposal-policy.test.ts` — blocks a task whose eligibility is `pending_confirmation`. Browser: the panel showed eligibility as _Awaiting confirmation_ with the product unconfirmed.                                                                                                                       |
| D02 | Confirmed and applicable shows the right products, quantity and version                             | Browser: panel showed _Confirmed affected_, instructions `v1`, one product row `Qty 1`. `tests/disposal-submit.integration.test.ts` asserts the task is seeded from the submitted products.                                                                                                                                                                                                                                                                                 |
| D04 | No approved, authorizing instruction ⇒ no permission, and no improvised method shown                | `tests/disposal-submit.integration.test.ts` — no task is opened for an unapproved version, nor for one whose only approval selects a measure that is not consumer disposal. No task means the consumer is never offered a disposal step.                                                                                                                                                                                                                                    |
| D05 | Approved content shows steps, examples, warnings and recognition requirements                       | Browser: rendered warning (before the steps), one numbered step, an example photo with alt text and caption, and the recognition checklist.                                                                                                                                                                                                                                                                                                                                 |
| D06 | An uploaded or technically verified file shows only technical progress                              | Browser: the upload control labelled the same evidence _Checking_ then _Passed our checks_, and never _accepted_.                                                                                                                                                                                                                                                                                                                                                           |
| D08 | Accepted evidence plus every condition ⇒ a permission tied to the right version, batch and products | Browser: clicked _Issue permission to dispose_; the database then held a `disposal_authorizations` row with `status: active`. `tests/disposal-service.integration.test.ts` covers the same transition with the gate refusing before acceptance.                                                                                                                                                                                                                             |
| D12 | A hold blocks the permission even when the photos are accepted                                      | `tests/disposal-service.integration.test.ts` — accepted evidence plus a hold is refused with `DISPOSAL_ON_HOLD`. Browser: placed a hold, confirmed the row changed to _Release the hold_, then released it and saw `released_at` set.                                                                                                                                                                                                                                       |
| D25 | A Notice of Violation alone does not enable consumer disposal                                       | Browser: recorded `materialType: nov` with an otherwise authorizing scope and measure; the server returned `authorizesConsumerDisposal: false` and the screen said _Recorded as history. It does not authorize consumer disposal, so no task will open._ Database: the stored row is `authorizes_consumer_disposal: false` and the version it backs authorizes nothing. `tests/disposal-guards.integration.test.ts` rejects the same combination when the flag is set true. |
| D26 | A Form 332 or CBP record does not authorize consumer disposal                                       | `tests/disposal-guards.integration.test.ts` — both are refused as authorizing, and both remain storable as history.                                                                                                                                                                                                                                                                                                                                                         |

## Implemented, unproven

| #   | Criterion                                                                               | What exists                                                                                                                                                                                                    | What is missing                                                                                                                        |
| --- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| D13 | Closing the reportability review does not lift the disposal hold                        | The hold is a row in `disposal_holds`, and nothing in the reportability path touches it; `tests/disposal-policy.test.ts` keeps the two independent.                                                            | No test closes a reportability review and then asserts the hold survives, and no browser run has done it.                              |
| D03 | An inapplicable recall skips Page 4 and demands no declaration                          | The claim flow enters the disposal step only when the submission returns a task, and the declaration is offered only from the policy's own action ids.                                                         | No end-to-end run of the _skip_ path: no browser session has submitted a claim with no task and confirmed no declaration is asked for. |
| D07 | Missing recognition ⇒ resubmission with a reason, no permission                         | The review decision takes a reason code and the refusal path is tested (`needs_resubmission` blocks the permission). The consumer-facing resubmission wording exists.                                          | The consumer's view of a resubmitted batch has not been rendered in a browser.                                                         |
| D09 | Replacing accepted photos re-reviews and the old permission cannot be reused            | `submitEvidenceBatch` supersedes earlier batches and suspends live authorizations.                                                                                                                             | No test asserts the suspension, and no browser run has resubmitted.                                                                    |
| D10 | Cross-user or cross-campaign access is refused                                          | Wrong-token reads and uploads are refused (`tests/disposal-upload.integration.test.ts`); `disposal_evidence_batch_documents.document_id` is unique, so a photo cannot satisfy two reviews.                     | No test attempts to attach another person's document.                                                                                  |
| D11 | An invalid or expired visitor credential cannot view, continue or declare               | Wrong credentials are refused on read, upload and declaration.                                                                                                                                                 | Expiry is enforced in code but never exercised; no test advances a task token past its expiry.                                         |
| D14 | A mixed-product case only authorises the confirmed, reviewed quantities                 | `disposal_task_products` carries per-product confirmation and quantity, and a batch document may name the product and quantity it covers.                                                                      | Nothing computes or asserts that a permission covers a subset — the authorization does not yet carry a quantity.                       |
| D15 | A withdrawn instruction stops an open page from acting                                  | Withdrawal suspends every live authorization for the version and supersedes pending batches (`withdrawInstruction`).                                                                                           | No test and no browser run: the command exists but has never been executed.                                                            |
| D16 | An unauthorised person cannot review, and an administrator cannot skip the gate         | Permissions are a closed set with an exhaustive matrix test; the agent gate has no force option.                                                                                                               | The admin console's disposal controls have not been exercised as a non-authorised role.                                                |
| D17 | Concurrent review, retry and duplicate declaration do not duplicate anything            | One decision per batch (`disposal_reviews_batch_uidx`), one live authorization per task and batch (partial unique index), declarations keyed by authorization.                                                 | No concurrent execution; the constraints are asserted only as schema shape.                                                            |
| D18 | Evidence may be reviewed before final submission, and a visitor may safely continue     | The upload path is task-credentialed and works on a submitted claim (`tests/disposal-upload.integration.test.ts`); the resume link is delivered by email.                                                      | The consumer-side continuation has not been run end to end.                                                                            |
| D19 | The applicable branch cannot claim completion without a valid permission or declaration | The contract, the service and a database CHECK each require exactly one basis; the service resolves the authorization itself.                                                                                  | The consumer-facing declaration control has been clicked only for the completion branch, and never against a task with no basis.       |
| D20 | A consumer who disposed early keeps a true timeline and is not blocked                  | The exception path records `already_disposed_before_authorization` with a note, and `tests/disposal-service.integration.test.ts` asserts no permission is manufactured.                                        | The exception form has not been submitted from the browser.                                                                            |
| D21 | History survives an instruction update or withdrawal                                    | Nothing deletes a decision, batch or declaration.                                                                                                                                                              | No test asserts retention across a withdrawal.                                                                                         |
| D22 | Old data is not silently reclassified, and reviewed evidence is not reaped              | The retention guard is tested (`tests/draft-cleanup-worker.integration.test.ts`): expired evidence awaiting review is kept, evidence past its window is reaped. Historical rows keep NULL for the new columns. | The migration's non-backfill is asserted only by inspection.                                                                           |
| D24 | Every decision is attributable, and a rollback keeps history                            | Disposal writes go through `audit.record`; withdrawal suspends rather than deletes.                                                                                                                            | No audit-trail test for a disposal action, and no rollback rehearsal (E3).                                                             |
| D27 | Examples never show laboratory damage and never require reproducing a test              | Not applicable to code: instructions are operator content.                                                                                                                                                     | Depends on the content pack, which does not exist yet.                                                                                 |
| D28 | A CAP choosing return or other compensation does not force disposal                     | The check constraint makes a non-consumer-disposal measure impossible to store as authorizing.                                                                                                                 | No test drives the whole flow with `measure: consumer_return`.                                                                         |
| D29 | A sample or import record does not imply approval for a whole series                    | Same constraint as D25/D26.                                                                                                                                                                                    | Not asserted for this input specifically.                                                                                              |

## Not built

| #   | Criterion                               | Note                                                                                                                                                                                                                                    |
| --- | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D23 | Mobile capture, keyboard, screen reader | The consumer surfaces use semantic lists, required alt text, `aria-hidden` decoration and native controls, but nothing has been tested with a keyboard or a screen reader, and the file input cannot be driven from the in-app browser. |

## What this means for §7.3

The P0 slice — D01–D08, D12–D16, D25–D29 — is largely proven at the service layer and,
for the consumer and admin surfaces that exist, in a browser. The honest gaps inside
that slice are D03's skip path, D07's consumer view, D13 end to end, D14 (a permission
does not yet carry a quantity), D15 (withdrawal has never been executed) and D16
(no non-authorised role has tried the controls).

None of those gaps is a known defect; each is a place where the code exists and nothing
would notice if it broke. Closing them is the next step, and each is small.

## Prerequisites this matrix cannot resolve

Real enablement is blocked on the business side, not here: the approval material that
permits consumer disposal, the content pack (approved steps, example photos, safety
warnings, declaration text), the evidence retention period, and the answer to who may
confirm a product as affected. No campaign has approved instructions, so the feature is
dark by construction rather than by configuration.
